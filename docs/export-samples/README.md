# Export samples

Step 10 introduces a publication/export contract. The tests generate example payload and manifest files in-memory, and this directory documents the expected output pairing:

- `<payload>.json`
- `<payload>.manifest.json`
