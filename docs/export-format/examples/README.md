# Export Format Examples

- `java-jpa-normalized-association-export.json` — curated sample payload showing the expected normalized JPA association shape, evidence retention, and relationship catalog exposure for downstream consumers.
