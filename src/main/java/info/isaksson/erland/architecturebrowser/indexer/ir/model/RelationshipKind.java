package info.isaksson.erland.architecturebrowser.indexer.ir.model;

public enum RelationshipKind {
    DEPENDS_ON,
    EXTENDS,
    IMPLEMENTS,
    EXPOSES,
    CALLS,
    READS,
    WRITES,
    CONTAINS,
    USES
}
